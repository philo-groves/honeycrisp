# Temporary Project Note

When implementing the memory plan, update `planning/MEMORY_PLAN.md` checklists as work lands. Keep each session scoped to the requested phase unless the user explicitly expands scope.

This note is temporary and can be removed once checklist tracking is folded into permanent project guidance.
